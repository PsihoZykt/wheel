# Changelog: Постоянные участники колеса

## Дата: 2025-11-01

### Добавлено

#### 1. Модели данных

**Файл**: `src/models/permanentParticipants.model.ts`

- `PermanentParticipant` - интерфейс для постоянного участника
  - `id`: уникальный идентификатор
  - `name`: имя участника
  - `amount`: вес участника (влияет на размер сектора)
  - `color`: цвет сектора в колесе
  - `enabled`: флаг активности (включен/выключен для прокрута)
- `PermanentParticipantsConfig` - интерфейс для конфигурации
  - `participants`: массив участников
  - `version`: версия конфига для совместимости

#### 2. Сервис управления участниками

**Файл**: `src/services/PermanentParticipantsService.ts`

Singleton-сервис для работы с постоянными участниками. Основные методы:

**Управление участниками:**

- `getParticipants()` - получить всех участников из localStorage
- `saveParticipants(participants)` - сохранить участников в localStorage
- `addParticipant(participant)` - добавить нового участника
- `removeParticipant(id)` - удалить участника
- `updateParticipant(id, updates)` - обновить данные участника
- `toggleParticipant(id)` - переключить состояние enabled/disabled
- `getEnabledParticipants()` - получить только включенных участников

**Конвертация:**

- `toWheelItems(participants?)` - конвертировать участников в формат WheelItem для колеса

**Импорт/Экспорт:**

- `exportConfig()` - экспорт конфига в JSON строку
- `importConfig(configJson, merge)` - импорт конфига из JSON строки
- `exportToFile()` - экспорт конфига в файл
- `importFromFile(file, merge)` - импорт конфига из файла

**Утилиты:**

- `clear()` - очистить всех участников
- `generateRandomColor()` - генерация случайного цвета для нового участника

**Хранилище:**

- Ключ localStorage: `permanent_participants`
- Версия конфига: `1.0`
- Автоматическая генерация UUID для участников (с fallback на библиотеку uuid)

#### 3. UI Компонент

**Файл**: `src/components/RandomWheel/WheelSettings/fields/PermanentParticipants.tsx`

Material-UI компонент для управления постоянными участниками.

**Функционал:**

- Список всех участников с индикацией состояния (включен/отключен)
- Диалог добавления/редактирования участника
- Удаление участника
- Включение/отключение через чекбокс
- Кнопка загрузки включенных участников в колесо

**Кнопки импорта/экспорта:**

- 📤 Импорт из файла
- 📥 Экспорт в файл
- 📋 Копирование в буфер обмена
- 📄 Вставка из буфера обмена
- ➕ Добавление участника

**Интеграция с уведомлениями:**

- Все операции сопровождаются уведомлениями (success/error)
- Использует Redux для отображения алертов

**Автоматическое обновление колеса:**

- При изменении состояния участников колесо обновляется автоматически
- Используется контекст WheelContext для взаимодействия с колесом

#### 4. React Hook

**Файл**: `src/hooks/usePermanentParticipants.ts`

Кастомный хук для упрощения работы с постоянными участниками.

**Возвращаемые значения:**

- `participants` - массив всех участников
- `enabledParticipants` - массив включенных участников
- `wheelItems` - участники в формате WheelItem
- `addParticipant()` - функция добавления
- `removeParticipant()` - функция удаления
- `updateParticipant()` - функция обновления
- `toggleParticipant()` - функция переключения enabled
- `exportToFile()` - экспорт в файл
- `importFromFile()` - импорт из файла
- `exportConfig()` - экспорт в строку
- `importConfig()` - импорт из строки
- `reload()` - перезагрузка данных

**Особенности:**

- Автоматическая перезагрузка при изменениях
- Мемоизация функций через useCallback
- Управление состоянием через useState и useEffect

#### 5. Интеграция в настройки колеса

**Файл**: `src/components/RandomWheel/WheelSettings/WheelSettings.tsx`

Компонент PermanentParticipantsField добавлен в настройки колеса:

- Отображается после настроек музыки
- Доступен во всех режимах колеса
- Не зависит от других настроек

#### 6. Документация

**Файлы:**

- `PERMANENT_PARTICIPANTS_GUIDE.md` - руководство пользователя (русский)
- `src/components/RandomWheel/WheelSettings/fields/README_PermanentParticipants.md` - техническая документация
- `CHANGELOG_PERMANENT_PARTICIPANTS.md` - этот файл с описанием изменений

### Технические детали

#### Хранение данных

- **Место**: localStorage браузера
- **Ключ**: `permanent_participants`
- **Формат**: JSON
- **Версионирование**: поле `version` в конфиге

#### Генерация ID

```typescript
const generateId = (): string => {
  if (typeof crypto !== 'undefined' && crypto.randomUUID) {
    return crypto.randomUUID();
  }
  return uuidv4(); // fallback
};
```

#### Генерация цветов

```typescript
private generateRandomColor(): string {
  const hue = Math.floor(Math.random() * 360);
  const saturation = 60 + Math.floor(Math.random() * 20);
  const lightness = 50 + Math.floor(Math.random() * 10);

  return `hsl(${hue}, ${saturation}%, ${lightness}%)`;
}
```

#### Формат конфига

```json
{
  "participants": [
    {
      "id": "uuid-here",
      "name": "Participant Name",
      "amount": 1,
      "color": "hsl(180, 65%, 55%)",
      "enabled": true
    }
  ],
  "version": "1.0"
}
```

### Зависимости

Использованные библиотеки (уже присутствуют в проекте):

- `@mui/material` - UI компоненты
- `@mui/icons-material` - иконки
- `react-redux` - интеграция с Redux
- `uuid` - генерация UUID (fallback)

### Совместимость

- ✅ Работает во всех современных браузерах с поддержкой localStorage
- ✅ Совместимо с существующим функционалом колеса
- ✅ Не влияет на работу аукциона
- ✅ Можно использовать параллельно с импортом через ParticipantsImport

### Безопасность

- Данные хранятся только локально в браузере пользователя
- Нет отправки данных на сервер
- Валидация JSON при импорте
- Обработка ошибок с уведомлениями пользователю

### Производительность

- Минимальное влияние на производительность
- Данные загружаются только при монтировании компонента
- Операции с localStorage выполняются синхронно
- Автоматическое сохранение при каждом изменении (без debounce, т.к. операции редкие)

### Возможные улучшения (TODO)

1. Добавить режим "merge" при импорте (объединение с существующими)
2. Добавить возможность группировки участников (категории)
3. Добавить историю изменений с возможностью отката
4. Добавить валидацию имен (проверка на дубликаты)
5. Добавить поиск/фильтрацию в списке участников
6. Добавить массовое редактирование (изменение веса для всех)
7. Добавить шаблоны конфигов
8. Добавить синхронизацию между вкладками (BroadcastChannel API)
9. Добавить drag-and-drop для сортировки участников
10. Добавить статистику (сколько раз участник выигрывал)

### Тестирование

Для тестирования функционала:

1. Откройте приложение и перейдите к колесу
2. Прокрутите настройки до блока "Постоянные участники"
3. Добавьте нескольких участников
4. Протестируйте включение/отключение через чекбоксы
5. Загрузите участников в колесо
6. Экспортируйте конфиг в файл
7. Очистите localStorage и импортируйте конфиг обратно
8. Протестируйте копирование/вставку через буфер обмена

### Заключение

Функционал постоянных участников колеса полностью реализован и готов к использованию. Все компоненты покрыты TypeScript типами, следуют принятым в проекте паттернам и не нарушают существующую функциональность.
