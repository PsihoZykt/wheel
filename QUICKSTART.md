# 🚀 Quick Start Guide

## Быстрый старт после миграции на Next.js

### 1. Установка зависимостей

```bash
# Удалите node_modules и lock файлы (опционально, для чистой установки)
rm -rf node_modules yarn.lock

# Установите зависимости
yarn install
```

### 2. Настройка переменных окружения

Создайте файл `.env.local`:

```env
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_DOCS_BASE_URL=/docs
```

### 3. Запуск проекта

```bash
# Development режим
yarn dev

# Production сборка
yarn build
yarn start
```

Приложение будет доступно на: **http://localhost:3000**

## 🔍 Что проверить после запуска

### Критичные страницы:

- [ ] `/` - Главная страница (Аукцион)
- [ ] `/settings` - Настройки
- [ ] `/wheel` - Колесо фортуны
- [ ] `/history` - История
- [ ] `/statistic` - Статистика

### Интеграции:

- [ ] Twitch авторизация (`/twitch/redirect`)
- [ ] DA авторизация (`/da/redirect`)
- [ ] DonatePay подключение

### Функциональность:

- [ ] Redux store работает корректно
- [ ] WebSocket соединения устанавливаются
- [ ] API запросы проходят через прокси
- [ ] Стили загружаются правильно
- [ ] i18n переключение языков работает

## 🐛 Возможные проблемы и решения

### Проблема: "window is not defined"

**Решение**: Добавьте `'use client'` в начало файла компонента

```tsx
'use client';

import { useEffect } from 'react';
// ... rest of the code
```

### Проблема: Ошибки импорта модулей

**Решение**: Проверьте пути в `tsconfig.json` и убедитесь, что алиасы настроены правильно

### Проблема: Стили не применяются

**Решение**: Убедитесь, что SCSS файлы импортируются корректно и PostCSS настроен

### Проблема: API запросы не проходят

**Решение**: Проверьте `next.config.mjs` rewrites и убедитесь, что backend запущен

## 📝 Важные изменения

### Навигация

**Было (React Router):**

```tsx
import { useNavigate, useLocation, Link } from 'react-router-dom';

const navigate = useNavigate();
navigate('/path');

<Link to='/path'>Link</Link>;
```

**Стало (Next.js):**

```tsx
import { useRouter, usePathname } from 'next/navigation';
import Link from 'next/link';

const router = useRouter();
router.push('/path');

<Link href='/path'>Link</Link>;
```

**Или используйте утилиты совместимости:**

```tsx
import { useNavigate, useLocation } from '@utils/navigation';
import Link from '@components/Navigation/Link';

// Работает как в React Router
const navigate = useNavigate();
navigate('/path');

<Link to='/path'>Link</Link>;
```

### Environment Variables

**Было (Vite):**

```tsx
import.meta.env.VITE_API_URL;
import.meta.env.MODE;
```

**Стало (Next.js):**

```tsx
process.env.NEXT_PUBLIC_API_URL;
process.env.NODE_ENV;
```

### Client/Server Components

По умолчанию все компоненты в `app/` - **серверные**.

Добавьте `'use client'` если компонент использует:

- React hooks (useState, useEffect, etc.)
- Browser APIs (window, localStorage, etc.)
- Event handlers (onClick, onChange, etc.)
- Redux hooks (useSelector, useDispatch)

## 🛠️ Полезные команды

```bash
# Проверка типов
yarn type-check

# Линтинг с автофиксом
yarn lint --fix

# Анализ bundle size
yarn build && yarn analyze

# Очистка кеша Next.js
rm -rf .next

# Полная переустановка
rm -rf node_modules .next yarn.lock
yarn install
```

## 📚 Дополнительная информация

- Подробная миграция: [MIGRATION.md](./MIGRATION.md)
- Полная документация: [README.md](./README.md)
- Next.js Docs: https://nextjs.org/docs

## ✅ Чеклист готовности к production

- [ ] Все тесты проходят
- [ ] Нет TypeScript ошибок
- [ ] Линтер не выдает критичных ошибок
- [ ] Все интеграции работают
- [ ] API запросы проходят корректно
- [ ] Оптимизированы изображения (next/image)
- [ ] Настроены метаданные для SEO
- [ ] Проверена производительность (Lighthouse)
- [ ] Настроен CI/CD
- [ ] Созданы environment variables для production

## 🎯 Следующие шаги

1. **Запустите проект**: `yarn dev`
2. **Протестируйте основной функционал**
3. **Проверьте документацию**: `yarn docs:dev`
4. **Исправьте найденные проблемы**
5. **Подготовьте production deployment**

Удачи! 🚀
