# TODO После Миграции

## 🔴 Критичные задачи (нужно сделать перед первым запуском)

### 1. Обновить компоненты с React Router
Файлы, требующие обновления:

- [ ] `src/utils/changelog.tsx`
- [ ] `src/pages/settings/NewSettingsPage.tsx`
- [ ] `src/components/HomePage/HomePage.tsx`
- [ ] `src/components/Event/Aukus/AukusRedirectPage.tsx`
- [ ] `src/components/ConfirmFormOnLeave/ConfirmFormOnLeave.tsx`
- [ ] `src/components/ChatWheelPage/ChatWheelPage.tsx`
- [ ] `src/components/Metadata/StructuredData.tsx`
- [ ] `src/components/Metadata/Essentials.tsx`
- [ ] `src/components/Form/SlotsPresetInput/SlotsPresetInput.tsx`

**Действия:**
- Заменить импорты из `react-router-dom` на `next/navigation` или использовать утилиты из `@utils/navigation`
- Обновить `<Link to="">` на `<Link href="">` или использовать `@components/Navigation/Link`
- Заменить `<Navigate>` на компонент из `@components/Navigation/Navigate`

### 2. Проверить использование browser APIs

Найти компоненты с:
```bash
# В терминале:
grep -r "window\." src/ --include="*.tsx" --include="*.ts"
grep -r "document\." src/ --include="*.tsx" --include="*.ts"
grep -r "localStorage" src/ --include="*.tsx" --include="*.ts"
```

**Действия:**
- Добавить `'use client'` директиву
- Обернуть в проверку: `if (typeof window !== 'undefined')`

### 3. Удалить старые файлы

- [ ] `src/main.tsx` (заменен на `src/app/layout.tsx`)
- [ ] `src/App.tsx` (логика перенесена в `src/app/page.tsx` и `MainLayout.tsx`)
- [ ] `vite.config.ts`
- [ ] `tsconfig.node.json`
- [ ] `index.html` (корневой)

## 🟡 Важные задачи (желательно сделать в ближайшее время)

### 4. Оптимизация изображений
- [ ] Заменить `<img>` на `<Image>` из `next/image`
- [ ] Настроить remote patterns в `next.config.mjs` для внешних изображений

### 5. SEO оптимизация
- [ ] Добавить metadata для каждой страницы
- [ ] Создать `robots.txt` в `public/`
- [ ] Создать динамический `sitemap.xml`

### 6. API Routes (опционально)
Вместо прокси можно создать API routes:
- [ ] `src/app/api/[...proxy]/route.ts` - универсальный прокси
- [ ] Или отдельные routes для каждого API endpoint

### 7. Тестирование
- [ ] Протестировать все страницы
- [ ] Проверить все интеграции (Twitch, DA, DonatePay)
- [ ] Проверить WebSocket соединения
- [ ] Проверить работу Redux
- [ ] Тест загрузки/сохранения слотов
- [ ] Тест колеса фортуны
- [ ] Тест аукциона

## 🟢 Улучшения (можно сделать позже)

### 8. Performance оптимизация
- [ ] Настроить dynamic imports для тяжелых компонентов
- [ ] Использовать `React.lazy` и `Suspense` где возможно
- [ ] Проверить bundle size: `yarn build && yarn analyze`
- [ ] Оптимизировать загрузку шрифтов через `next/font`

### 9. Code splitting
- [ ] Вынести тяжелые библиотеки в отдельные чанки
- [ ] Использовать динамические импорты для модалок
- [ ] Оптимизировать загрузку editor компонентов (TipTap)

### 10. Улучшение типизации
- [ ] Добавить типы для всех environment variables
- [ ] Создать строгие типы для API responses
- [ ] Улучшить типизацию Redux store

### 11. Документация
- [ ] Обновить inline документацию
- [ ] Добавить примеры использования новых утилит
- [ ] Создать гайд по добавлению новых страниц

### 12. CI/CD
- [ ] Настроить GitHub Actions для автоматической сборки
- [ ] Добавить автоматическое тестирование
- [ ] Настроить deploy на Vercel/Netlify

## 📋 Конкретные файлы требующие обновления

### Высокий приоритет:

```typescript
// src/utils/changelog.tsx
// Использует: useLocation, useNavigate
// Действие: Заменить на next/navigation или утилиты

// src/pages/settings/NewSettingsPage.tsx  
// Использует: Routes, Route
// Действие: Убрать Routes/Route, использовать вложенные страницы app router

// src/components/ChatWheelPage/ChatWheelPage.tsx
// Использует: useParams
// Действие: Заменить на useParams из next/navigation
```

### Средний приоритет:

```typescript
// src/components/ConfirmFormOnLeave/ConfirmFormOnLeave.tsx
// Использует: useLocation, useBlocker
// Действие: Нужно найти альтернативу для Next.js

// src/components/Metadata/*
// Использует: useLocation
// Действие: Заменить на usePathname
```

## 🔧 Скрипты для помощи

### Найти все импорты React Router:
```bash
grep -r "from 'react-router" src/ --include="*.tsx" --include="*.ts"
```

### Найти использование window:
```bash
grep -r "window\." src/ --include="*.tsx" --include="*.ts" | grep -v "typeof window"
```

### Найти использование document:
```bash
grep -r "document\." src/ --include="*.tsx" --include="*.ts"
```

### Проверить наличие 'use client' в нужных файлах:
```bash
# Файлы с hooks без 'use client'
grep -l "useState\|useEffect" src/**/*.tsx | xargs grep -L "'use client'"
```

## ✅ Готово

- [x] Базовая структура Next.js
- [x] Конфигурация TypeScript
- [x] Настройка Redux
- [x] Создание всех страниц
- [x] Настройка стилей (SCSS, Mantine, MUI)
- [x] i18n конфигурация
- [x] Утилиты для совместимости с React Router
- [x] Error и Loading страницы
- [x] Документация по миграции

## 📝 Примечания

- Документация VitePress оставлена как есть и работает отдельно
- API прокси настроено в `next.config.mjs`
- Все path aliases сохранены
- Redux middleware сохранены

---

**Дата миграции:** 2025-11-01
**Next.js версия:** 15.0.0
**React версия:** 18.3.0

