# 🔧 Исправления после миграции

## Исправление: "process is not defined"

### Проблема
```
ReferenceError: process is not defined
```

Возникала при попытке доступа к `process.env` в файлах, которые загружаются на клиенте до полной инициализации.

**Обновление:** Ошибка также возникала в самом `env.ts` файле из-за немедленной инициализации константы `ENV`.

### Решение

#### 1. Создан модуль для безопасной работы с env переменными

**Файл:** `src/utils/env.ts`

```typescript
// Безопасный доступ через getters (ленивая инициализация)
export const ENV = {
  get API_URL(): string {
    return safeGetEnv('NEXT_PUBLIC_API_URL', 'http://localhost:8000');
  },
  get DOCS_BASE_URL(): string {
    return safeGetEnv('NEXT_PUBLIC_DOCS_BASE_URL', '/docs');
  },
  get NODE_ENV(): string {
    return safeGetEnv('NODE_ENV', 'development');
  },
};
```

**Почему getters?** 
Getters вычисляются только при обращении к свойству, а не при импорте модуля. Это позволяет избежать ошибки `process is not defined` на клиенте.

#### 2. Обновлены все файлы с прямым доступом к process.env

**До:**
```typescript
// ❌ Может вызвать ошибку на клиенте
baseUrl: process.env.NEXT_PUBLIC_DOCS_BASE_URL || '/docs'
```

**После:**
```typescript
// ✅ Безопасный доступ
import { ENV } from '@utils/env';
baseUrl: ENV.DOCS_BASE_URL
```

#### 3. Добавлены проверки существования process

**Для функций:**
```typescript
export const isProduction = (): boolean => {
  if (typeof process !== 'undefined' && process.env) {
    return process.env.NODE_ENV === 'production';
  }
  return false;
};
```

### Исправленные файлы:
- ✅ `src/constants/config.ts`
- ✅ `src/utils/common.utils.ts`
- ✅ `src/assets/i18n/index.ts`
- ✅ Создан `src/utils/env.ts`

### Рекомендации для будущего

**Всегда используйте один из этих подходов:**

#### Вариант 1: Использовать модуль ENV
```typescript
import { ENV } from '@utils/env';

const apiUrl = ENV.API_URL;
```

#### Вариант 2: Добавить проверку
```typescript
const isDev = typeof process !== 'undefined' && process.env?.NODE_ENV === 'development';
```

#### Вариант 3: Использовать в компонентах с 'use client'
```typescript
'use client';

// В client components process.env доступен
const apiUrl = process.env.NEXT_PUBLIC_API_URL;
```

### ⚠️ Важно помнить

1. **NEXT_PUBLIC_*** переменные доступны на клиенте
2. Переменные без **NEXT_PUBLIC_** доступны только на сервере
3. При импорте модулей они выполняются сразу, до инициализации React
4. Используйте `ENV` константу для стабильного доступа

## Проверка

После исправления проект должен запускаться без ошибок:

```bash
yarn dev
```

Если возникают другие ошибки с `process`, примените тот же подход:
1. Добавьте проверку `typeof process !== 'undefined'`
2. Или используйте `ENV` из `@utils/env`
3. Или добавьте `'use client'` в компонент

---

**Статус:** ✅ Исправлено  
**Дата:** 2025-11-01

