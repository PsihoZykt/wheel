# 🎯 НАЧНИТЕ ОТСЮДА

## Быстрый старт после миграции

### Шаг 1: Установка зависимостей

```bash
yarn install
```

### Шаг 2: Создайте .env.local

```bash
cat > .env.local << 'EOF'
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_DOCS_BASE_URL=/docs
EOF
```

### Шаг 3: Запустите проект

```bash
yarn dev
```

Откройте: **http://localhost:3000**

---

## 📖 Документация

1. **[MIGRATION_SUMMARY.md](./MIGRATION_SUMMARY.md)** - Что было сделано
2. **[QUICKSTART.md](./QUICKSTART.md)** - Детальная инструкция по запуску
3. **[MIGRATION.md](./MIGRATION.md)** - Полное руководство по миграции
4. **[TODO_AFTER_MIGRATION.md](./TODO_AFTER_MIGRATION.md)** - Что нужно доделать
5. **[README.md](./README.md)** - Основная документация

---

## ⚠️ ВАЖНО: Что нужно исправить перед использованием

### Критичные задачи:

1. **Обновить компоненты с React Router**

   Найдите и обновите файлы, использующие `react-router-dom`:

   ```bash
   grep -r "from 'react-router" src/ --include="*.tsx" --include="*.ts"
   ```

   **Быстрое решение:** Используйте утилиты совместимости из `src/utils/navigation.ts`

2. **Добавить 'use client' в компоненты с hooks**

   Найдите компоненты с hooks без директивы:

   ```bash
   grep -l "useState\|useEffect" src/**/*.tsx | xargs grep -L "'use client'"
   ```

   Добавьте `'use client'` в начало таких файлов.

3. **Проверить browser APIs**
   ```bash
   grep -r "window\." src/ --include="*.tsx" | grep -v "typeof window"
   ```

---

## 🎯 Следующие шаги

1. ✅ Запустите `yarn dev`
2. ✅ Откройте http://localhost:3000
3. ✅ Проверьте главную страницу
4. ✅ Проверьте навигацию
5. ✅ Протестируйте основной функционал
6. ⚠️ Исправьте проблемы из TODO_AFTER_MIGRATION.md

---

## 🆘 Если что-то не работает

### Ошибка: "window is not defined"

```tsx
// Добавьте в начало файла:
'use client';
```

### Ошибка: "useNavigate is not defined"

```tsx
// Замените импорт:
// import { useNavigate } from 'react-router-dom';
import { useNavigate } from '@utils/navigation';
```

### Ошибка: "process is not defined"

```tsx
// Используйте безопасную утилиту:
import { ENV } from '@utils/env';

const apiUrl = ENV.API_URL;
```

См. подробности в [FIXES.md](./FIXES.md)

### Ошибка при сборке

```bash
# Очистите кеш и переустановите:
rm -rf .next node_modules yarn.lock
yarn install
yarn dev
```

---

## 📝 Основные изменения

| Старое (Vite + React Router) | Новое (Next.js)             |
| ---------------------------- | --------------------------- |
| `import.meta.env.VITE_*`     | `process.env.NEXT_PUBLIC_*` |
| `useNavigate()`              | `useRouter()` или утилита   |
| `<Link to="">`               | `<Link href="">`            |
| `<Navigate to="">`           | `router.push()`             |

---

## 📞 Помощь

Если возникли проблемы, смотрите:

- [FIXES.md](./FIXES.md) - Исправленные проблемы и решения
- [QUICKSTART.md](./QUICKSTART.md) - Детальные инструкции
- [TODO_AFTER_MIGRATION.md](./TODO_AFTER_MIGRATION.md) - Известные проблемы

Удачи! 🚀
